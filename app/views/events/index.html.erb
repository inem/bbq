<h1 class="mt-5"><%= I18n.t("views.event.index") %></h1>

<div class="mt-5">
  <%= link_to I18n.t("views.event.new"), new_event_path, class: 'btn btn-success' %>
</div>

<div class="row mb-5">
  <%= render partial: 'events/event', collection: @events %>
</div>
