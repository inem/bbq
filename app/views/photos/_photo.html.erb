<% owner = photo.user %>
<% image = photo.photo %>
<%= link_to (image_tag(image.variant(:medium))), url_for(image), alt: t("images.alt.photo"), class: "my-2 col-md-4",
  data: {lightbox: "photo-galery", title: link_to((render partial: "users/user", object: owner), user_path(owner)) }%>


